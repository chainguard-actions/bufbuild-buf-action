name: Test buf-action with exclude_paths input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: buf
        uses: .
        with:
          setup_only: "false"
          lint: "true"
          format: "false"
          breaking: "false"
          push: "false"
          archive: "false"
          input: "${{ github.workspace }}/tests/fixtures/proto"
          exclude_paths: |
            acme/v1/hello.proto
      - name: Assert buf_version output is set
        env:
          BUF_VERSION: ${{ steps.buf.outputs.buf_version }}
        run: |
          echo "buf_version: $BUF_VERSION"
          if [ -z "$BUF_VERSION" ]; then
            echo "FAIL: buf_version output is empty"
            exit 1
          fi
          echo "PASS: lint with exclude_paths completed, buf_version=$BUF_VERSION"
